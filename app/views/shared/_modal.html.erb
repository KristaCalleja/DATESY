<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
  Match!
</button>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Why are you recommending?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= simple_form_for [@friend, @match], url: user_friend_matches_path do |f| %>
          <%= f.input :content, label: 'Why?' %>
          <%= f.input :matchee_id, as: :hidden, input_html: { value: matchee.id } %>
          <%= f.button :submit, 'Match!', class: "button" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
